services:
  nginx:
    image: nginx:alpine
    container_name: pos-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - /etc/ssl/pos:/etc/ssl/pos:ro  # mount certs
    depends_on:
      - frontend
      - backend
    networks:
      - pos-network
    restart: unless-stopped
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pos-backend
    expose:
      - "5000"
    environment:
      - FLASK_ENV=production
      - GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/credentials.json
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SECRET_KEY=${SECRET_KEY}
    volumes:
      - ./secrets/credentials.json:/run/secrets/credentials.json:ro
    networks:
      - pos-network
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_BACKEND_API_URL: /api
        VITE_BUSINESS_NAME: ${VITE_BUSINESS_NAME}
        VITE_BUSINESS_RUC: ${VITE_BUSINESS_RUC}
        VITE_BUSINESS_ADDRESS: ${VITE_BUSINESS_ADDRESS}
        VITE_PRINT_BRIDGE_URL: ${VITE_PRINT_BRIDGE_URL}
    container_name: pos-frontend
    expose:
      - "80"
    depends_on:
      - backend
    networks:
      - pos-network
    restart: unless-stopped

networks:
  pos-network:
    driver: bridge